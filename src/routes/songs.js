const express = require('express');

const router = express.Router();

//READ ALL
router.get('/', () => { });

//GET BY ID
router.get('/:id', () => { });

//CREATE
router.post('/', () => { });

//DELETE
router.delete('/:id', () => { });

//UPDATE
router.put('/:id', () => { });

//FAVORITE
router.patch('/:id/favorite', () => { });
